{
  "private": true,
  "packageManager": "yarn@3.3.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --parallel",
    "lint": "turbo run lint",
    "codegen": "yarn workspace @survey/utils codegen"
  },
  "devDependencies": {
    "eslint": "8.29.0",
    "eslint-config-custom": "workspace:*",
    "prettier": "^2.8.1",
    "turbo": "^1.6.3"
  }
}
